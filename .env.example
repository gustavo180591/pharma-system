# Database
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/pharma_system?schema=public"

# App
NODE_ENV=development

# Auth (generate a secure secret)
AUTH_SECRET=your-secret-key-here
AUTH_TRUST_HOST=true

# Optional: Email configuration (for password reset, etc.)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=user@example.com
# SMTP_PASSWORD=your-password
# EMAIL_FROM=noreply@pharma-system.local
